DATABASE_URL=
DIRECT_URL=
AUTH_SECRET=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_APP_URL=
FEED_SYNC_API_KEY=

# Serper is configured as a secret in Netlify production, never commit the actual key.
SERPER_API_KEY=
# Opt-in paid page extraction fallback (charges apply per scraped URL).
SERPER_SCRAPE_ENABLED=false

# Explicit Prysight tenant receiving Syntrx data. Never use an implicit default tenant.
PRYSIGHT_SYNTRX_COMPANY_ID=
