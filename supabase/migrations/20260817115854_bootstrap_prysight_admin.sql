-- Historical version marker.
-- User credentials are deliberately not bootstrapped through a database migration.
-- Provision the first super admin through the authenticated application flow or an
-- explicitly authorized one time operational procedure.
select 1;
